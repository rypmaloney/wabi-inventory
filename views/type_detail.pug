extends layout

block content
    div.list
        h1= title
        

        each category in category_list
            div.list-item
                a(href=category.url) #{category.name}


        else
            p There are no items.
    div.add
        form(action='/inventory/category/create')
            button.btn.btn-primary(type='submit') Add new category